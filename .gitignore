
# --- runtime / volatile ---
logs/*.log
state/*.json
state/*.bak*
*.bak
*.tmp
tmp/

# runtime report output
reports/waiting_report.txt

# === runtime / live state ===
feeds/work_events_email.live.jsonl
archive/drop_processed/

# --- generated apply outputs (never commit) ---
/reports/APPLIED_HARDENED_*.xlsm
/reports/APPLIED_HARDENED_*.audit.jsonl
/run_apply.sh

# --- generated batch artifacts ---
/reports/BATCH_APPLY_*.csv

# --- signatures are generated outputs ---
/reports/signatures/

# --- local helper scripts if you don't want them committed ---
# (comment these out if you DO want them committed)
/run_apply_batch.sh
/sign_outputs.sh
/dryrun_diff_report.py
/audit_schema_guard.py
